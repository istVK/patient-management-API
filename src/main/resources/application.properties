spring.application.name=patientmanagementapi
# ====================
# Database Configuration
# ====================
spring.datasource.url=jdbc:mysql://localhost:3306/patient_management
spring.datasource.username=root
spring.datasource.password=*12253
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


##DOCKER

#spring.datasource.url=jdbc:mysql://mysql:3306/${DB_NAME}
#spring.datasource.username=${DB_USER}
#spring.datasource.password=${DB_PASSWORD}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ====================
# Hibernate / JPA
# ====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# Server port (optional if already 8080)
server.port=8080

